/* ============================================================================
 *  PyroOS  -  cooperative multitasking
 * ==========================================================================*/
#ifndef TASK_H
#define TASK_H

/* Run the scheduler demo: spawn several tasks that interleave via yielding,
   then return once they have all finished. */
void tasking_demo(void);

#endif
